{
  "name": "@monerium/openapi",
  "version": "2.4.0",
  "description": "Monerium OpenAPI specification",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/monerium/js-monorepo.git",
    "directory": "packages/openapi"
  },
  "bugs": {
    "url": "https://github.com/monerium/js-monorepo/issues"
  },
  "main": "./openapi.yml",
  "files": [
    "openapi.yml",
    "README.md"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "dev": "redocly preview-docs ./openapi.yml",
    "lint": "redocly lint --extends recommended openapi.yml && pnpm run sort",
    "format": "prettier --write \"**/*.{yml,yaml}\"",
    "sort": "openapi-format openapi.yml -o openapi.yml && pnpm run format",
    "pub:pre": "pnpm publish --no-git-checks --dry-run",
    "clean": "rm -rf dist node_modules .turbo"
  },
  "devDependencies": {
    "@redocly/cli": "^1.25.0",
    "openapi-format": "^1.18.0"
  },
  "engines": {
    "node": ">=18"
  }
}
